# Codeception tests example.

### Setup

- Clone repo
- Install dependencies:
    - `composer install`

### To test

- Run selenium
- Run Codeception, installed via Composer 

```
./vendor/bin/codecept run
```
